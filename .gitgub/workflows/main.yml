name: A workflow for my Hello World file
on: push
jobs:
  build:
    name: A simple pipeline action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "Enes"
